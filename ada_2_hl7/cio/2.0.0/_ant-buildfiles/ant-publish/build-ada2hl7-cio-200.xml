<?xml version="1.0" encoding="UTF-8"?>
<!-- Apache ANT build file that sets up (target = setup) or by default converts transaction with references to full ada xml (target = resolve-refs)
    Apache ANT is based on Java and may be found here: https://ant.apache.org/
    run on command line or from a batch file: 
        ant [-f build.xml] [target]
        
    If you just run ant, it expects a build.xml in the working dir and runs the default target.
-->
<project basedir="." default="convert_ada_2_hl7_cio200" name="build-ada2hl7-cio-200-include" xmlns:if="ant:if" xmlns:unless="ant:unless">
    <property name="is.version" value="2.0.0"/>
    <property name="is.version.short" value="200"/>
    <property name="date.T" value="2020-06-08"/>
  
    <!-- place import after properties, otherwise it can't resolve the properties used. -->
    <import file="../../../_ant-buildfiles/ant-publish/build-ada2hl7-cio-include.xml"/>

    <property name="prodinst.dir" value="C:\SVN\art_decor\trunk\ada-data\production-ada-instances\${is.abbreviation}\${is.version}\data"/>
    
    <target name="setup_cio200" description="Setup subdirectories, schemas, ada instance xml's in ${build.dir} for ${is.name} ${is.version}">

        <!-- generic stuff for all cio versions -->
        <antcall target="setup"/>
        
        <!-- cio 2.0.0 specific stuff -->
        <!-- beschikbaarstellen_contraindicatie copies schemas for project dir and appropriate ada instances from prodinst.dir -->
        <antcall target="setup_sub_cio">
            <param name="usecase" value="${usecase.bci}"/>
            <param name="file.include.string" value="bci-*"/>
        </antcall>

        <!-- let's convert to hl7 too while we're at it -->
        <antcall target="convert_ada_2_hl7_cio200"/>

    </target>

      <target name="convert_ada_2_hl7_cio200">

        <!-- generic stuff for all versions -->
        <antcall target="convert_ada_2_hl7"/>
          
          <!-- cio 2.0.0 specific stuff -->
          <!-- beschikbaarstellen_contraindicatie -->
          

    </target>

   </project>
