<?xml version="1.0" encoding="UTF-8"?>
<default_input>
    <!-- XML used as default input for ant calls to XSLT's where we are really inputting an ada collection, so it serves as a dummy input -->
    <dummy/>
</default_input>
